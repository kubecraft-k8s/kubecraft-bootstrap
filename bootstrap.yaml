apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: github.com-kubecraft-kubecraft-bootstrap
  namespace: flux-system
spec:
  interval: 1m
  url: https://github.com/kubecraft-k8s/kubecraft-bootstrap
  ref:
    branch: main
---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: kubecraft-bootstrap
  namespace: flux-system
spec:
  chart:
    spec:
      chart: chart
      interval: 1m
      sourceRef:
        kind: GitRepository
        name: github.com-kubecraft-kubecraft-bootstrap
        namespace: flux-system
  interval: 1m
  values:
    velero:
      keyid: 0042a4ce0ece8c00000000020
      accesskey: K004kxEoFaLMONaeb7kUrPjyPorqPqs
      awsbucket: kubecraft-test
      awsregion: us-west-004
      s3url: s3.us-west-004.backblazeb2.com


